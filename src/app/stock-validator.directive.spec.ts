import { StockValidatorDirective } from './stock-validator.directive';

describe('StockValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new StockValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
